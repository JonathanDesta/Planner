{
  "name": "campus-planner",
  "version": "2.0.5",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node scripts/serve.js",
    "test": "node --test tests/*.test.js",
    "test:browser": "node tests/browser.mjs && node tests/sync-browser.mjs",
    "format": "prettier --write js tests scripts index.html styles.css sw.js manifest.json package.json *.md",
    "format:check": "prettier --check js tests scripts index.html styles.css sw.js manifest.json package.json *.md",
    "check:deployment": "node scripts/check-deployment.js"
  },
  "devDependencies": {
    "playwright": "1.62.1",
    "prettier": "3.6.2"
  }
}
